# AGENTS.md - qtclass

## 版本发布

### 标签命名

| 范围 | 格式 | 示例 |
|------|------|------|
| 仓库级 | `vX.Y.Z` | `v0.0.2` |
| 子包 | `<子包>/vX.Y.Z` | `studio/v0.0.2` |

### CHANGELOG

- 根 `CHANGELOG.md` 只记仓库级大变更，一行一条，不列细节
- 子包版本见各自 `CHANGELOG.md`（如 `src/studio/CHANGELOG.md`）
- v0.x 阶段根 CHANGELOG 从简，v1.0.0+ 后再充实
